---
"@querycn/table-react": minor
---

First release of `@querycn/table-react`: `useDataTable` with sorting, pages and search in the URL, a saved column layout (order, visibility, pinning, widths, colors) and `@querycn/filter` integration.
